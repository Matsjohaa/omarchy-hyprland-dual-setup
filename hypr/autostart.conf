# ~/.config/hypr/autostart.conf
# Reloads Hyprland plugins and starts two Waybars (one per monitor)
exec-once = hyprpm reload -n
exec-once = bash -lc "sleep 1; pkill waybar; waybar -c $HOME/.config/waybar/monitors/waybar-1.jsonc & waybar -c $HOME/.config/waybar/monitors/waybar-2.jsonc &"
